$('#messages').append('<%= j render(@message) %>');

$("#new_message")[0].reset();

$('.flash-messages').html("<%= j (content_tag :div, flash[:notice], :id => 'flash_#{name}', :class => 'alert alert-success') %>");

